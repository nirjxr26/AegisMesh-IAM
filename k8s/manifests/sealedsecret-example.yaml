apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: aegismesh-secrets
  namespace: aegismesh
spec:
  encryptedData: {}

# NOTE:
# This file is a placeholder example. You must generate a real SealedSecret using the
# `kubeseal` client and the installed SealedSecrets controller public key.
# Example:
#  kubeseal --controller-namespace kube-system --format yaml < k8s/manifests/secret.yaml > k8s/overlays/prod/sealedsecret-aegismesh.yaml
