# Frontend Scripts

- `maintenance/fix-settings-imports.cjs` keeps the settings page import paths aligned after file moves.
- Add future one-off maintenance or migration helpers here instead of the repository root.